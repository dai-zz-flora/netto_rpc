<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:task="http://www.springframework.org/schema/task" xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd">
    <description>spring任务调度，quartz任务调度</description>

    <!-- Quartz集群Schduler -->
    <bean id="clusterQuartzScheduler" class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <!-- quartz配置文件路径, 指向cluster配置 -->
        <property name="configLocation" value="classpath:prop/quartz.properties" />
        <!-- 保存Job数据到数据库所需的数据源 -->
        <property name="dataSource" ref="dataSource" />
        <!-- 启动时延期2秒开始任务 -->
        <property name="startupDelay" value="2" />
        <!-- Triggers集成 -->
        <property name="triggers">
            <list>
                <ref bean="scannerJobTrigger" />
            </list>
        </property>
        <property name="jobFactory">
            <bean class="com.sprucetec.deploy.schedule.job.ScheduledJobFactory" />
        </property>
    </bean>
    
    <!-- 定时任务 -->
    <bean id="scannerJobTrigger" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
        <property name="jobDetail" ref="scannerJobDetail" />
        <!-- 每15秒执行一次 -->
        <property name="cronExpression" value="*/15 * * * * ? " />
    </bean>
    <bean id="scannerJobDetail" class="org.springframework.scheduling.quartz.JobDetailFactoryBean">
        <property name="durability" value="true" />
        <property name="jobClass" value="com.sprucetec.deploy.schedule.job.ScannerJobDetail" />
       <!--  <property name="jobDataAsMap">
        <map>
            <entry key="jobService" ref="jobServiceImpl"/>
            <entry key="clusterQuartzScheduler" ref="Scheduler"/>
        </map>
    	</property> -->
    </bean>
</beans>